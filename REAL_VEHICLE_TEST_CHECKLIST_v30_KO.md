# BYD Dolphin Auto Assistant v30 실차 검증 체크리스트

이 문서는 v30에서 아직 실차 의미 매핑이 필요한 항목만 모은 것입니다. 이미 제공한 자료를 다시 요청하지 않습니다. 주행 중 조작하지 말고, 반드시 안전한 정차 상태에서 동승자가 기록하거나 운행을 마친 뒤 로그를 내보내십시오.

## 먼저 준비할 것

1. v30을 설치하고 앱을 한 번 실행합니다.
2. Android 13 이상이면 알림 권한을 허용합니다.
3. 테스트할 기능만 켭니다. 계기판 TBT와 실험적 전방 출발 후보 감지는 기본 OFF입니다.
4. 앱의 **DPI & 원터치 진단 → 15분 수집 시작**을 누릅니다. 스마트폰 버그제거 앱이나 별도 로그 앱은 필요하지 않습니다.
5. 각 증상이 보이는 순간 **문제 순간 표시**를 누르고, 시험이 끝나면 **수집 종료 + ZIP 내보내기**를 누릅니다.
6. 결과 `DolphinAssistant_v30_DiagnosticSession_*.zip` 하나와 화면에서 관찰한 결과만 함께 보냅니다.

## A. 기본 진단 — 필수

- 시동 ON, P단, 정차 상태에서 15분 진단을 시작합니다.
- ZIP에는 BYD 권한, speed, gear, EPB, charging, ADAS 원시값, driveInterface, energyFeedback, 런타임 공개 메서드와 앱 오류가 자동으로 포함됩니다.
- 앱에서 실제로 사용한 기능과 실패한 버튼이 있다면 그 순간 **문제 순간 표시**를 누릅니다. 별도로 시각을 받아 적을 필요는 없습니다.

### 원터치 수집 기능 자체 확인

1. 시작 직후 상태가 초록색 **수집 중**으로 바뀌고 남은 시간이 줄어드는지 확인합니다.
2. **문제 순간 표시**를 한 번 눌러 기록 완료 안내가 나오는지 확인합니다.
3. 15분을 기다릴 필요 없이 1~2분 뒤 **수집 종료 + ZIP 내보내기**를 눌러도 됩니다.
4. 공유 화면에 `DolphinAssistant_v30_DiagnosticSession_*.zip`이 나타나면 성공입니다. ZIP 내부에는 `events.jsonl`, `diagnostic_report.txt`, `device_snapshot.json`, `framework_methods.txt`, `vehicle_initial_state.json`이 있어야 합니다.
5. 앱이 도중에 종료됐다면 다시 실행하십시오. **수집 완료 · ZIP 내보내기** 상태로 복구되는 것이 정상입니다.

## B. AVH 원시값 매핑 — 필요

안전한 평지에 완전히 정차하고 브레이크를 유지합니다.

1. AVH OFF 상태로 10초 유지합니다.
2. AVH를 차량 기본 UI/물리 조작으로 ON 합니다.
3. 정상적인 방법으로 오토홀드가 실제 체결되는 상태와 해제되는 상태를 각각 10초 유지합니다.
4. AVH를 OFF 합니다.

필요 로그는 `raw adas.avh` 전환입니다. 각 전환 시각과 계기판에 표시된 AVH 상태를 함께 적어 주십시오. 이 매핑 전에는 v30이 AVH 음성을 자동 실행하지 않습니다.

## C. BSD·차선·TJA 원시값 — 선택

- 정상 운행에서 자연스럽게 발생한 이벤트만 수집합니다.
- 이벤트를 만들기 위해 위험한 차선 접근이나 사각지대 상황을 고의로 만들지 마십시오.
- 가능하면 동승자가 계기판 상태와 시각만 기록합니다.
- 필요 로그: `raw adas.bsd`, `raw adas.laneOffset`, `raw adas.tja`.

## D. 주행 모드·회생제동 매핑 — 필요

안전하게 정차한 상태에서 차량 기본 UI로 모드만 한 단계씩 바꿉니다.

- 가능한 각 주행 모드를 차례로 선택하고 5초씩 유지합니다.
- 가능한 각 회생제동 단계를 차례로 선택하고 5초씩 유지합니다.
- 선택한 표시명, 순서, 시각을 적습니다.
- 필요 로그: `raw instrument.driveInterface`, `raw instrument.energyFeedback`.

## E. 계기판 TBT 표시 — 필요

1. 정차 상태에서 **계기판 TBT**를 켭니다.
2. 앱의 시험 입력으로 우회전, 350 m, 다음 도로명을 보냅니다.
3. 계기판에 화살표·거리·도로명이 각각 나타났는지 기록합니다.
4. 끄기 후 표시가 사라지는지 확인합니다.

필요 자료는 계기판 관찰 결과와 같은 시점의 진단 로그입니다. 운전 중 시험 버튼을 누르지 마십시오.

## F. 티맵 Plus HUD 프로토콜 — 장비 연동을 원할 때만 필요

제공된 본체 라벨로 제품은 `TMAP Plus HUD / T900`, 제조사는 주식회사 인포라텍, KC 등록은 `R-R-9IT-JARVIS3000`으로 확인했습니다. v30은 HUD 패킷을 보내지 않습니다.

먼저 T900을 차량 Android와 정상 페어링한 뒤 원터치 진단을 실행하십시오. 앱이 다음 항목을 자동 수집합니다.

- 전체 페어링 기기의 장치명, 앞 3바이트를 가린 주소, 유형과 현재 UUID
- T900 후보에 대한 공개 SDP UUID 갱신 결과
- Bluetooth 본딩, ACL 연결·해제, 이름과 UUID 변경 이벤트
- 앱의 HUD SPP 연결 시도 결과와 예외

이 ZIP만으로 장치 탐색·권한·SPP 연결 문제는 대부분 분석할 수 있습니다. 실제 화면 데이터·밝기·오디오 명령을 구현하려면 펌웨어 버전과 제조사 프로토콜 문서가 가장 좋습니다. 문서가 없다면 제조사 공식 앱 조작 중의 Android Bluetooth HCI snoop `.pcap`/`.cfa`가 별도로 필요하지만, Android 보안상 일반 앱이 이 원시 패킷을 직접 수집할 수는 없습니다.

계정 비밀번호, 인증 토큰, 전화번호 등 개인정보는 보내지 마십시오. 캡처에 포함되었다면 먼저 삭제하거나 마스킹하십시오.

## G. 2분할 실패가 있을 때만 필요

1. 서로 다른 두 앱을 선택합니다.
2. 50:50으로 실행한 뒤 30:70, 70:30을 각각 시도합니다.
3. AVM을 열었다 닫아 마지막 비율이 복원되는지 확인합니다.
4. 실패한 경우에만 앱 이름, 선택 비율, 화면 결과와 `SPLIT` 태그가 포함된 진단 파일을 보냅니다.

## H. 충전 상태 전환 — 가능할 때 선택

- 충전 대기 → 충전 시작 → 정상 종료 순서에서 각각 시각을 기록합니다.
- 필요 로그: `raw charging.work`, `raw charging.state`, `raw charging.gun`.
- 충전기를 일부러 비정상 종료하거나 안전장치를 우회하지 마십시오.

## 보내면 좋은 최소 묶음

- `DolphinAssistant_v30_DiagnosticSession_*.zip`
- TBT/2분할 화면 관찰 결과 한 줄씩
- T900 화면 송신까지 구현하려는 경우에만 펌웨어 정보 또는 HCI snoop 파일

이 정도면 AVH, 주행 모드, 회생제동, 계기판 수용 여부를 안전하게 확정하고 다음 버전에서 자동 음성·표시를 연결할 수 있습니다.
