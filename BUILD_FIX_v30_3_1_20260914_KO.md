# v30.3.1 GitHub Actions 빌드 오류 수정

## 원인
`MainActivity.kt`의 `setupSafetyAudioSubScreen()`에서 아래 함수를 호출하지만 함수 본문이 누락되어 Kotlin 컴파일이 실패했습니다.

- `showBsdLdpConfigDialog("BSD 안내음 미리듣기 설정", isBsd = true)`
- `showBsdLdpConfigDialog("차선 안내음 미리듣기 설정", isBsd = false)`

GitHub Actions 오류:
- MainActivity.kt:563:13 `Unresolved reference: showBsdLdpConfigDialog`
- MainActivity.kt:566:13 `Unresolved reference: showBsdLdpConfigDialog`

## 수정
직전 v30.3 정상 소스의 `showBsdLdpConfigDialog(title: String, isBsd: Boolean)` 구현을 v30.3.1에 복원했습니다.

복원 기능:
- BSD/LDP 비프 모드 선택 및 미리듣기
- 추천 TTS 선택
- 사용자 직접 문구 입력/저장/미리듣기

v30.3.1의 오디오 경로 테스트 UI 가시성 개선 코드는 그대로 유지했습니다.

## 검증
- `showBsdLdpConfigDialog` 호출 2개 + 정의 1개 존재 확인
- MainActivity.kt 중괄호 개수 일치 확인
- 기존 v30.3 구현과 동일한 함수 본문 복원
- 이 실행 환경에서는 Gradle wrapper가 services.gradle.org DNS에 접근하지 못해 전체 assembleDebug 재빌드는 실행할 수 없었습니다. GitHub Actions에서 최종 컴파일 확인 필요.
