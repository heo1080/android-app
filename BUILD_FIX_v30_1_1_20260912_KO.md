# v30.1.1 빌드 오류 수정

사용자가 제공한 GitHub Actions 로그에서 `:app:compileDebugKotlin`이 `BydPermissionContext.kt` 때문에 실패했습니다.

## 원인
Android `Context`/`ContextWrapper`의 다음 메서드는 `permission` 인수가 non-null `String`인데, v30.1 소스가 `String?`으로 선언하여 Kotlin override 대상과 일치하지 않았습니다.

- `enforceCallingOrSelfPermission`
- `checkCallingOrSelfPermission`
- `enforcePermission`
- `checkPermission`

이 때문에 `overrides nothing` 4건과, nullable `String?`을 `String` 인수에 전달한 type mismatch 4건이 함께 발생했습니다.

## 수정
네 메서드의 `permission: String?`을 `permission: String`으로 변경했습니다. `message`는 nullable 상태를 유지했습니다.

버전:
- versionCode: 32
- versionName: `3.0.2-v30.1.1-neon-buildfix`

## 검증
- 해당 파일 내 잘못된 nullable permission override가 남아 있지 않음
- 앱 전체에서 동일한 Permission override 패턴이 더 없음
- Android XML 파싱 성공
- Kotlin 파일의 기본 괄호/문자열 구조 검사 성공
- `R.id` 참조와 XML 선언 교차검사 수행

전체 Android Gradle 빌드는 이 작업 환경의 외부 다운로드 제한 때문에 실행하지 못하므로 GitHub Actions에서 `./gradlew :app:assembleDebug --stacktrace`로 최종 확인해야 합니다.
